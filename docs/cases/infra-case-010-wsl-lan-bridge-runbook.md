---
status: canonical
last_reviewed: 2026-06-09
scope: ecosystem
# --- generated by obsidian-case-registry.ps1 (do not edit; derived from ## Run State) ---
app: infra
run_status: COMPLETE
tier: 2
next_agent: done
branch: dev
interrupted: false
updated: 2026-06-09
tags: [app/infra, status/complete, tier/2]
---

# CASE: infra-case-010-wsl-lan-bridge-runbook

WSL2 Docker dev stack healthy inside VM but `https://192.168.100.7` unreachable from Windows/LAN
after WSL or Docker restart — Windows↔WSL portproxy bridge drift, not app code.

Related: [[infra-case-006-dynamic-lan-host]]

## Vault links

- Registry: [[CASE_REGISTRY]] · Contracts: [[CONTRACTS_HUB]] · Home: [[OPERATOR_HOME]]
- Related cases: [[infra-case-006-dynamic-lan-host]]

## Run State

- task_slug: infra-case-010-wsl-lan-bridge-runbook
- tier: 2
- branch: dev
- status: COMPLETE
- last_completed_agent: executioner
- next_agent: done
- active_runner: cursor
- interrupted: false
- interrupt_reason: none
- updated: 2026-06-09

## Problem

After `wsl --shutdown` or reboot, WSL2 VM IP changes. Stale `netsh portproxy` rules forward
`0.0.0.0:80/443/4443` to the old WSL address → browser "Site cannot be reached" at
`https://192.168.100.7` even when Docker/nginx are healthy inside WSL.

## Success Criterion

All seven operator checks pass; logon scheduled task registered; runbook documents daily
`pld sync` + when to run `pld network` manually.

## Specialist Investigation & Implementation

### Diagnostic evidence (2026-06-09)

| Check | Result |
| ----- | ------ |
| WSL `curl -sk https://localhost/login` | HTTP 200 |
| `openssl` SAN on `docker/certs/fullchain.pem` | `IP Address:192.168.100.7` present |
| WSL IP (`hostname -I`) | `172.21.115.211` |
| `netsh interface portproxy show v4tov4` | `0.0.0.0:80/443/4443 → 172.21.115.211` (matches WSL IP) |
| `curl.exe -ksf https://192.168.100.7/login` | HTTP 200 |
| `curl.exe -ksf https://192.168.100.7:4443/` | HTTP 200 |
| Scheduled task `Woosoo-WSL-LAN-Bridge` | State: Ready (not yet run since register — LastTaskResult 267011 = never executed) |
| Firewall `Woosoo-WSL-Dev` | Enabled, TCP 80/443/4443 inbound, Private+Public |

### Fix applied

**Fix A (portproxy)** — already correct at diagnosis time; no stale rules. Root cause class
confirmed: when IP drifts, `pld network` / `setup-wsl-lan-access.ps1` is the remedy.

**Fix B (certs)** — not needed; SAN includes `192.168.100.7`.

**Fix C (firewall)** — not needed; `Woosoo-WSL-Dev` rule present and enabled.

### Changes this session

1. **`scripts/windows/register-wsl-lan-startup-task.ps1`** — runs `setup-wsl-lan-access.ps1`
   immediately after task registration (elevated session already open).
2. **`docs/USAGE_GUIDE.md`** — new § WSL LAN bridge runbook (daily flow, one-time task, post-`wsl --shutdown`).
3. **`docs/deployment/DEPLOYMENT_GUIDE.md`** — runbook cross-link in §4.1.1.

Pre-existing (untracked, verified complete): `register-wsl-lan-startup-task.ps1`,
`unregister-wsl-lan-startup-task.ps1`, `setup-wsl-lan-access.ps1`.

## Code Simplification

SKIPPED — new scripts only; no dead code introduced, no existing code paths touched.

## Hygiene (dead-code-cleanup)

SKIPPED — new files added, nothing removed or obsoleted. No orphaned imports, helpers, or
debug logs introduced. `recurrence-check.ps1` confirms all scripts parse clean (CHK-PS-PARSE).

## Verifier

**Verdict: PASS** (2026-06-09)

| # | Check | Result |
|---|-------|--------|
| 1 | `Get-ScheduledTask 'Woosoo-WSL-LAN-Bridge'` state | **Ready** |
| 2 | Task trigger | `MSFT_TaskLogonTrigger` (at logon) |
| 3 | Task principal | `RunLevel=Highest`, `LogonType=Interactive` |
| 4 | Task action | `powershell.exe -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File "…\setup-wsl-lan-access.ps1"` |
| 5 | `netsh portproxy show v4tov4` | `0.0.0.0:80/443/4443 → 172.21.115.211` (current WSL IP) |
| 6 | `curl.exe https://192.168.100.7/login` | HTTP 200 |
| 7 | `curl.exe https://192.168.100.7:4443/` | HTTP 200 |
| 8 | PS AST parse: both scripts | Syntax clean; `Set-StrictMode + $ErrorActionPreference = Stop` |
| 9 | `setup-wsl-lan-access.ps1` + `teardown-wsl-lan-access.ps1` exist | True |

Non-blocking findings:
- `LastTaskResult 267011` (SCHED_S_TASK_HAS_NOT_RUN) — expected; at-logon task fires on next Windows login, not immediately.
- Task action path hardcoded at registration time via `$PSScriptRoot`; re-run needed if repo path changes.

## Recurrence Check

`scripts\recurrence-check.ps1` — **6/6 PASS** (2026-06-09)

```
[PASS] CHK-PS-ASCII         - 17 scripts ASCII-only
[PASS] CHK-PS-PARSE         - 17 scripts parse clean
[PASS] CHK-STATUS-CLASSIFY  - registry anchored; regression test 9/9 PASS
[PASS] CHK-WIKILINK-RELATIVE - 132 docs; 0 relative wikilinks
[PASS] CHK-CANVAS-TRACKED   - 2 hub canvases committable
[PASS] CHK-REGISTRY-SUMMARY - no frontmatter key leaked as case summary
```

## Documentation Sync

Specialist updated docs as part of implementation (no separate scribe pass required for ops-only task):

| File | Change |
|------|--------|
| `docs/USAGE_GUIDE.md` | Added `pld network` + `register-wsl-lan-startup-task.ps1` to command reference; added portproxy-avoidance note |
| `docs/deployment/DEPLOYMENT_GUIDE.md` | Added §4 LAN bridge runbook: when portproxy stales, one-time task registration, daily workflow, cross-link |

Both sections describe behaviour the scripts actually implement and match the verified scheduled task configuration.

## Files Changed

- `scripts/windows/register-wsl-lan-startup-task.ps1`
- `docs/USAGE_GUIDE.md`
- `docs/deployment/DEPLOYMENT_GUIDE.md`
- `docs/cases/infra-case-010-wsl-lan-bridge-runbook.md` (this file)

## Contract impact

No — platform scripts and operator docs only.

## Rollback

Unregister task: `scripts\windows\unregister-wsl-lan-startup-task.ps1`; revert doc edits.

## Agent Chain

- Tier: 2
- Branch: dev
- Contrarian: Tier 2 classification
- Specialist: infra — portproxy automation scripts + doc updates
- Code Simplifier: SKIPPED (new scripts only, no dead code introduced)
- Hygiene (dead-code-cleanup): SKIPPED (new files; recurrence-check CHK-PS-PARSE confirms clean)
- Verifier: PASS
- Docs Sync: Included in Specialist phase (USAGE_GUIDE.md + DEPLOYMENT_GUIDE.md updated)
- Executioner: APPROVED

## Executioner Verdict

APPROVED (2026-06-09)

All Tier 2 gate requirements satisfied. Specialist stayed within allowed paths; scripts verified
clean (StrictMode, idempotent, immediate-apply); Verifier confirmed live HTTP 200 on both LAN
endpoints; recurrence-check 6/6 PASS; documentation sync explicitly checkpointed.

Non-blocking follow-up: the task fires at logon — first real validation is after next reboot.
If `https://192.168.100.7/login` is unreachable after reboot, run `pld network` once (UAC).
